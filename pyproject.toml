[project]
name = "pyyaml-include"
version = "1.1.3"
description = ""
readme = "README.md"
requires-python = ">=3.10"
authors = [
  { name="Liu Xue Yan", email="liu_xue_yan@foxmail.com" },
  { name="Tiago Taveira-Gomes", email="tiago.taveira@sigil-enterprises.com" },
]
dependencies = [
  "PyYAML~=6.0",
  "fsspec>=2021.04.0",
  "typing-extensions; python_version<'3.11'",
]

[project.urls]
Homepage = "https://github.com/sigil-enterprises/pyyaml-include"
Issues = "https://github.com/sigil-enterprises/pyyaml-include/issues"

[project.optional-dependencies]
dev = [
  "pyyaml-include[test]",
  "pyyaml-include[docs]",
  "pyyaml-include[ci]",
  "pytest-watch",
]
test = [
  "pytest",
  "pyodbc",
]
docs = [
  "toml",
  "mike",
  "mkdocs",
  "mkdocs-material",
  "mkdocstrings-python",
]
ci = [
  # ``coverage-badge`` depends on ``pkg_resources`` which was removed
  # in Python 3.13 / modern setuptools. ``genbadge[coverage]`` is the
  # fleet-standard replacement; same SVG output, parses
  # ``coverage.xml`` instead of a coverage data file.
  "genbadge[coverage]",
  # ``coverage[toml]`` is needed for ``coverage xml`` to read pyproject.
  "coverage[toml]",
]

[tool.coverage.html]
directory = "docs/reports/coverage"

[tool.coverage.run]
data_file = "var/.coverage"
omit = [
  "tests/*",
  "/usr/*",
]

[tool.coverage.report]
exclude_also = [
  "raise AssertionError",
  "raise NotImplementedError",
  "except PackageNotFoundError:",
]

[build-system]
requires = [
  "setuptools >= 61.0",
  "setuptools-scm >= 8.0",
]
build-backend = "setuptools.build_meta"
